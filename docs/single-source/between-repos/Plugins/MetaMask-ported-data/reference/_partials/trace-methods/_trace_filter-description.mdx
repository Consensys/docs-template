


Returns traces matching the specified filter. The block range is limited to 100 blocks per query (the difference between `fromBlock` and `toBlock`). {/* <CreditCost network="evm_trace" method="trace_filter" />... - Component not available */}

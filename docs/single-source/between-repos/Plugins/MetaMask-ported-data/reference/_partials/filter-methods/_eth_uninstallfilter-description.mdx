


Uninstalls a filter with given ID. This method should always be called when watching is no longer needed.
Additionally, filters time out when they aren't requested with
`eth_getFilterChanges` for a period of time. {/* <CreditCost network="evm_filter" method="eth_uninstallFilter" />... - Component not available */}
